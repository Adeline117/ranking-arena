# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ARENA ingest worker — local Playwright browser profiles (never commit;
# also keeps Tailwind v4 auto content-detection from scanning cached
# exchange HTML and generating broken bg-[url(...)] utilities)
.arena-ingest/

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/
/lighthouse-ci-report/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
!**/.env.template

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# data backups
/data/backup/
/data/snapshots/

# 临时数据文件
binance_copy_trading_*.json
bitget_*.json
bybit_*.json
mexc_*.json
coinex_*.json

# Capacitor / Native App
# Android
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/*.iml
android/.idea/
*.keystore
android/app/google-services.json

# iOS
ios/App/Pods/
ios/App/App.xcworkspace/xcuserdata/
ios/App/build/
ios/DerivedData/
*.xcuserstate
ios/App/App/GoogleService-Info.plist

# APNs keys
*.p8
secrets/

# Cloudflare Worker
cloudflare-worker/node_modules/

# Worker secrets + logs
worker/.env
worker/logs/

# Infrastructure
infra/*/node_modules/
infra/*/package-lock.json

# Supabase CLI temp
supabase/.temp/

# Generated reports and artifacts
playwright-report/
data/generated/
scripts/output/

# Local scripts (may contain sensitive data)
scripts/local-scrape.mjs
.env*.local
ranking-arena.db
/*.db
/data/*.db
/--version/
/logs/
scripts/monitoring/reports/

# Compiled fetchers
lib/cron/fetchers/*.js
lib/cron/fetchers/*.js.map
lib/cron/fetchers/*.d.ts
lib/cron/fetchers/*.d.ts.map

# Foundry (Smart Contract Development)
/broadcast/
/cache/
/contracts/out/
foundry.lock
backups/

# Temporary/generated files
*.bak
*.backup
*.old-working
debug_*.png
lighthouse-report.*
.lighthouse-report.json
docs/lighthouse-results-*.json
screenshots/
.archived/
.swc/

# Ad-hoc browse/screenshot sessions with unset output dir (2026-07 cleanup)
/undefined/

# Runtime STATE files — tooling mutates these on every run; tracking them made
# git status perpetually dirty across all sessions (2026-07-03 cleanup).
scripts/openclaw/.health-monitor-state.json
scripts/qa/.exhaustive-ledger.jsonl
scripts/qa/.exhaustive-ledger-auth.jsonl
scripts/qa/.queue-idle-check.mjs

# Ad-hoc QA/scan outputs
results.json
*.log

# One-off scripts and ad-hoc test runners
check_gaps.mjs
convert-img-to-image.sh
install.sh
financial_publications_summary.md
arena-*-test.mjs
/test-*.mjs
quiz-*-test.mjs

# E2E test artifacts (screenshots, debug specs)
e2e/*-screenshots/
e2e/*debug*
/shots/
/shots-auth/

# Claude Code config
/.claude/*
!/.claude/skills/
!/.claude/commands/
!/.claude/agents/
!/.claude/hooks/
!/.claude/settings.json
!/.claude/ARENA_SKILL_SYSTEM.md
.backup-*
# trigger.dev (tracked — Phase 1 canary)
# trigger.config.ts
# lib/jobs/trigger.ts

# Throwaway QA/verification scratch scripts (root) — never commit
/verify-*.mjs
/watch-*.mjs
/vg.mjs
/t[0-9]*.mjs
scratchpad/
