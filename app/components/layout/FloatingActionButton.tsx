'use client'

// FloatingActionButton removed as part of Groups system cleanup.
// Post creation was tied to groups; a standalone post creation flow
// can be added later if needed.
export default function FloatingActionButton() {
  return null
}
