import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Groups | Arena', description: 'Enter. Outperform. Join trading discussion groups on Arena.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
