---
description: 80-item visual design audit across 10 categories — report only, never modifies code
---

Read `.claude/skills/arena-design-audit/SKILL.md` and execute the full design audit.

If the user provided arguments: $ARGUMENTS — focus the audit on those specific pages or components.
If no arguments, audit the full core path (homepage, rankings, trader detail, search, auth).

This is a READ-ONLY audit. Do NOT modify any code.
