---
description: QA lead — systematically test Arena, find bugs, auto-fix, commit atomically, produce health score
---

Read `.claude/skills/arena-qa/SKILL.md` and execute the QA process.

If the user provided arguments: $ARGUMENTS — use as tier (quick/standard/exhaustive) or focus area.
If no arguments, ask the user which tier they want.

Fix bugs found, commit each fix atomically, and produce a health score report.
