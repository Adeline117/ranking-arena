---
description: Headless browser — navigate Arena pages, take screenshots, test interactions, check responsive layouts
---

Read `.claude/skills/arena-browse/SKILL.md` and use headless browser to test Arena pages.

If the user provided arguments: $ARGUMENTS — use as the URL path or action to perform.
If no arguments, take screenshots of all core path pages (homepage, rankings, trader detail, search).

Ensure dev server is running first (check with curl localhost:3000).
