---
description: Design QA with auto-fix — find visual inconsistencies, spacing issues, AI slop and fix them
---

Read `.claude/skills/arena-qa-design/SKILL.md` and execute the design QA process.

If the user provided arguments: $ARGUMENTS — focus on those components or pages.
If no arguments, scan the full codebase for design inconsistencies.

Fix issues found, commit each fix atomically.
