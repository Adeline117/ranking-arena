# 按钮功能测试清单

## 测试状态图例
- ✅ 已测试通过
- ⚠️ 需要手动测试
- ❌ 存在问题

---

## 1. 帖子相关功能

### 1.1 点赞/踩 (PostFeed, GroupPage)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 点赞 | `POST /api/posts/[id]/like` | ⚠️ | 需要登录检查 |
| 踩 | `POST /api/posts/[id]/like` | ⚠️ | reaction_type: 'down' |
| 取消点赞/踩 | `POST /api/posts/[id]/like` | ⚠️ | 再次点击切换状态 |

**逻辑检查点：**
- [x] 未登录时显示提示
- [x] 防重复提交（processingRef）
- [x] 乐观更新（本地状态先更新）
- [x] 计数正确更新

### 1.2 投票 (PostFeed)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 看涨(bull) | `POST /api/posts/[id]/vote` | ⚠️ | |
| 看跌(bear) | `POST /api/posts/[id]/vote` | ⚠️ | |
| 观望(wait) | `POST /api/posts/[id]/vote` | ⚠️ | |
| 取消投票 | `POST /api/posts/[id]/vote` | ⚠️ | 再次点击相同选项 |

**逻辑检查点：**
- [x] 需要登录
- [x] 防重复提交
- [x] 百分比计算正确
- [x] 投票结果实时更新

### 1.3 评论 (PostModal)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 获取评论 | `GET /api/posts/[id]/comments` | ⚠️ | |
| 发表评论 | `POST /api/posts/[id]/comments` | ⚠️ | |
| 删除评论 | `DELETE /api/posts/[id]/comments` | ⚠️ | 仅限自己的评论 |
| 回复评论 | `POST /api/posts/[id]/comments` | ⚠️ | parent_id 参数 |

**逻辑检查点：**
- [x] 内容长度验证 (1-2000字)
- [x] 空内容检查
- [x] 评论计数更新

### 1.4 收藏 (PostActions)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 检查收藏状态 | `GET /api/posts/[id]/bookmark` | ⚠️ | |
| 添加收藏 | `POST /api/posts/[id]/bookmark` | ⚠️ | 支持收藏夹选择 |
| 取消收藏 | `POST /api/posts/[id]/bookmark` | ⚠️ | |

**逻辑检查点：**
- [x] 默认收藏夹自动创建
- [x] 收藏状态同步

---

## 2. 交易员相关功能

### 2.1 关注 (TraderHeader, UserPage)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 检查关注状态 | `GET /api/follow` | ⚠️ | |
| 关注 | `POST /api/follow` | ⚠️ | action: 'follow' |
| 取消关注 | `POST /api/follow` | ⚠️ | action: 'unfollow' |

**逻辑检查点：**
- [x] 需要登录
- [x] 关注数更新
- [x] 重复关注处理

---

## 3. 群组相关功能

### 3.1 加入/退出群组 (GroupPage)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 加入群组 | 直接 Supabase 操作 | ⚠️ | |
| 退出群组 | 直接 Supabase 操作 | ⚠️ | |

---

## 4. 通用功能

### 4.1 翻译 (PostFeed)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 翻译内容 | `POST /api/translate` | ⚠️ | |
| 显示原文 | 本地状态切换 | ⚠️ | |

### 4.2 转发/分享 (PostActions)
| 功能 | API 路由 | 状态 | 备注 |
|------|----------|------|------|
| 转发帖子 | `POST /api/posts/[id]/repost` | ⚠️ | |

---

## 5. 需要修复的问题

### 已发现问题：
1. _(暂无)_

### 建议改进：
1. 添加全局 loading 状态指示器
2. 统一错误提示样式
3. 添加网络断开时的离线提示

---

## 6. 测试说明

### 如何进行测试：
1. 启动开发服务器：`npm run dev`
2. 打开浏览器访问各页面
3. 按照清单逐项测试每个按钮功能
4. 检查控制台是否有错误
5. 检查网络请求是否正确

### 测试账号：
- 需要在 Supabase 中创建测试账号
- 或使用现有的账号进行测试

---

*最后更新: 2026-01-15*

