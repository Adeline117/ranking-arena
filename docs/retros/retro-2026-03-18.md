# Arena Retro — 2026-03-18

## Summary
Massive week: 748 commits, 133K insertions, 157K deletions. Major work included unified connector framework migration (all 24+ platforms), Crawlee scraper reference, trigger.dev Phase 1 setup, i18n completion (ja/ko 100%), and a standard-tier QA pass that found and fixed a critical compute-leaderboard bug. Pipeline stabilization dominated the first half of the week with timeout tuning and connector fixes.

## Metrics

| Metric | Last Week | This Week | Trend |
|--------|-----------|-----------|-------|
| Commits | N/A (first retro) | 748 | -- |
| Pipeline health | healthy | healthy | = |
| Type errors | 12 | 0 | +++ |
| TODO/FIXME count | N/A | 4 | -- |
| Active platforms | 24 | 28+ | + |
| Files created | N/A | 515 | -- |
| Files deleted | N/A | 620 | -- |
| Lines added | N/A | 133,081 | -- |
| Lines deleted | N/A | 157,356 | -- |

## What Went Well
- **Critical bug caught by QA**: compute-leaderboard was using wrong column names (`trader_key`/`period` instead of `source_trader_id`/`season_id`), meaning arena_score sync to v2 was completely broken. Fixed immediately.
- **Connector framework fully deployed**: All 24 active platforms now on the unified Connector architecture with Redis rate limiting, lazy initialization, and standardized normalize() output.
- **i18n 100% coverage**: Japanese and Korean translations completed (3977/3977 keys each). Only 20 keys were missing (pricing FAQ + sharing features).
- **Infrastructure expansion**: trigger.dev Phase 1 (6 core scheduled tasks), Crawlee scraper reference (all 13 endpoints), Meilisearch sync cron.
- **Zero TypeScript errors**: Down from 12 errors in follow API routes to 0 across entire codebase.
- **Pipeline stabilization**: OKX geo-block fix, scraper queue overflow fix, VPS proxy key trimming, connector timeout tuning.

## What Could Be Better
- **Connector timeout churn**: Multiple commits adjusting timeouts (120s, 240s, 600s, back to 120s). Need standardized timeout strategy per platform tier.
  - Action: Define 3 tiers (fast/medium/slow) with preset timeouts in connector capabilities.
- **Rollback needed once**: `5a8b0506` rolled back unified-connector migration. Canary testing should catch these before full rollout.
  - Action: Always run 24h canary on 2 platforms before batch migration.
- **Dead code accumulation**: TraderPageClient.tsx (551 lines) is completely unused. Dead code detected in QA.
  - Action: Delete or wire up TraderPageClient.tsx.
- **Phase 5 fabricated metrics**: compute-leaderboard estimates win_rate/MDD from ROI alone when data is missing. Displayed as real data.
  - Action: Add `is_estimated` flag or confidence_penalty to estimated metrics.

## Hotspots
- `app/api/cron/batch-fetch-traders/route.ts` — 40 changes (most changed file). Core dispatcher, changed with every connector/group adjustment.
- `app/api/cron/compute-leaderboard/route.ts` — 30 changes. Critical bug found here. Complex scoring logic.
- `lib/i18n/*.ts` — 113 combined changes. Translation work.
- `app/trader/[handle]/TraderProfileClient.tsx` — 25 changes. Complex component, double API call bug fixed.
- `lib/cron/enrichment-runner.ts` — 25 changes. Timeout tuning.
- `lib/connectors/platforms/bybit-futures.ts` — 15 changes. VPS scraper integration instability.

## Action Items
- [ ] Delete or integrate TraderPageClient.tsx (dead code) — P2
- [ ] Add `is_estimated` flag to Phase 5 fabricated win_rate/MDD — P2
- [ ] Define standard timeout tiers for connector capabilities — P3
- [ ] Run `/qa exhaustive` for full edge case coverage — P3
- [ ] Review Phase 5 estimation logic with product team — P2

## Pipeline Health
- Status: healthy
- Database: pass (1161ms)
- Redis: pass (2181ms)
- API: pass (2182ms)
- Active platforms: 28+
- Cron jobs: 75 in vercel.json

## QA Health Score
- Score: 88/100
- Bugs found: 11 (2 critical, 3 high, 4 medium, 2 low)
- Bugs fixed: 7
- Remaining: 4 (need user input on business logic decisions)
