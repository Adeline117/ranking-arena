# ─────────────────────────────────────────────────────────────
# .tsc-legacy-errors.txt
#
# Regex patterns (one per line, extended regex / `grep -E`) that mark
# TypeScript errors as LEGACY — known-broken files that we do not want
# to block pushes.
#
# Lines starting with `#` and blank lines are ignored by the loader in
# `.git/hooks/pre-push`.
#
# ★ 2026-07-02（企业级差距整改 F1）：全量 tsc 实测 0 错误，历史上的
#   166 条豁免全部是死条目（对应错误早已修复但名单从未清理），一次性清零。
#   从现在起本文件的目标状态是【空】——类型债零豁免：
#   1. 新增豁免前必须先尝试修复；实在要加，必须写明理由 + 计划删除日期。
#   2. 每次有人想加条目，就是 CI/pre-push 在正确地拦截一个真回归——
#      "把错误加进名单"不是修复（PM-202606-migration-drift 的同构教训：
#      豁免名单只会单向膨胀成盲区，除非默认态是空）。
#   3. 下面的占位模式永不匹配，仅为兼容不同 grep 对空模式文件的行为差异。
#
__TSC_LEGACY_PLACEHOLDER_NEVER_MATCHES__
